# 复制为 .env.android 后填写实际部署地址。该文件会被 Git 忽略。
VITE_API_BASE_URL=https://your-host.example/api
VITE_WS_BASE_URL=wss://your-host.example
VITE_BUILD_OUT_DIR=dist-android

# APP 请求可能经过移动网络，适当放宽超时时间。
VITE_API_TIMEOUT=45000
